<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Business Profile</title>
    <link rel="stylesheet" href="/stylesheets/profile.css">
</head>
<body>
    

        <!-- Right Column: Form Fields -->
        <div class="right-column">
            <h2>Business Profile</h2>
            <form action="/profile" method="post">
                <div class="form-group">
                    <label for="businessName">Business Name</label>
                    <input type="text" id="businessName" name="businessName" value="<%= profile ? profile.businessName : '' %>" required>
                </div>
                <div class="form-group">
                    <label for="ownerName">Owner's Name</label>
                    <input type="text" id="ownerName" name="ownerName" value="<%= profile ? profile.ownerName : '' %>" required>
                </div>
                <div class="form-group">
                    <label for="gst">GST No.</label>
                    <input type="text" id="gst" name="gst" value="<%= profile ? profile.gst : '' %>" required>
                </div>
                <div class="form-group">
                    <label for="address">Business Address</label>
                    <input type="text" id="address" name="address" value="<%= profile ? profile.address : '' %>" required>
                </div>
                <div class="form-group">
                    <label for="phone">Mobile No.</label>
                    <input type="tel" id="phone" name="phone" value="<%= profile ? profile.phone : '' %>" required>
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" value="<%= profile ? profile.email : '' %>" required>
                </div>
                <h3 style="color: gold; width: 100%;">ID Verification</h3>
                <div class="form-group">
                    <label for="idType">Select ID Type</label>
                    <select id="idType" name="idType" required>
                        <option value="" disabled>Choose an option</option>
                        <option value="aadhar" <%= profile && profile.idType === 'aadhar' ? 'selected' : '' %>>Aadhar Card</option>
                        <option value="pan" <%= profile && profile.idType === 'pan' ? 'selected' : '' %>>PAN Card</option>
                        <option value="passport" <%= profile && profile.idType === 'passport' ? 'selected' : '' %>>Passport</option>
                    </select>
                </div>
                <button type="submit">Submit</button>
            </form>
            
        </div>
    </div>

    <script src="/javascripts/profile.js"></script>
</body>
</html>
